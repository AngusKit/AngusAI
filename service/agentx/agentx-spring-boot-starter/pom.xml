<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>cloud.xcan.agentx</groupId>
    <artifactId>xcan-angusai.agentx-parent</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>xcan-angusai.agentx-spring-boot-starter</artifactId>
  <name>AgentX :: Spring Boot Starter</name>
  <description>AgentX 自动装配 Starter，聚合所有核心模块并提供默认配置</description>

  <dependencies>
    <!-- Core -->
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-core</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- Observability -->
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-observability</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- Spring Boot -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
    </dependency>

    <!-- LangChain4j -->
    <dependency>
      <groupId>dev.langchain4j</groupId>
      <artifactId>langchain4j</artifactId>
      <exclusions>
        <exclusion>
          <groupId>org.apache.opennlp</groupId>
          <artifactId>opennlp-tools</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>dev.langchain4j</groupId>
      <artifactId>langchain4j-core</artifactId>
    </dependency>

    <!-- Lombok -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- Models -->
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-model-openai</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-model-anthropic</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-model-ollama</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-model-gemini</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-model-qwen</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-model-zhipu</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-model-deepseek</artifactId>
    </dependency>

    <!-- Tools -->
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-tool-http</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-tool-code</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-tool-database</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-tool-search</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-tool-file</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-tool-notify</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-tool-ai</artifactId>
    </dependency>

    <!-- Vector Storage -->
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-vectorstore-pgvector</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-vectorstore-milvus</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-vectorstore-qdrant</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-vectorstore-chroma</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-vectorstore-elasticsearch</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-vectorstore-weaviate</artifactId>
    </dependency>
    <dependency>
      <groupId>cloud.xcan.agentx</groupId>
      <artifactId>xcan-angusai.agentx-vectorstore-mariadb</artifactId>
    </dependency>
  </dependencies>
</project>
